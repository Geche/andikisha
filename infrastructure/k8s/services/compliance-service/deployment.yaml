apiVersion: apps/v1
kind: Deployment
metadata:
  name: compliance-service
  namespace: andikisha
  labels:
    app: compliance-service
    app.kubernetes.io/name: compliance-service
    app.kubernetes.io/part-of: andikisha-hr
    app.kubernetes.io/version: latest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: compliance-service
  template:
    metadata:
      labels:
        app: compliance-service
        app.kubernetes.io/name: compliance-service
        app.kubernetes.io/part-of: andikisha-hr
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8085"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: compliance-service
          image: andikisha/compliance-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8085
              protocol: TCP
            - name: grpc
              containerPort: 9085
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: SERVER_PORT
              value: "8085"
            - name: GRPC_PORT
              value: "9085"
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: andikisha-config
                  key: compliance-db-host
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: "andikisha_compliance"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: andikisha-secrets
                  key: compliance-db-username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: andikisha-secrets
                  key: compliance-db-password
            - name: RABBITMQ_HOST
              valueFrom:
                configMapKeyRef:
                  name: andikisha-config
                  key: rabbitmq-host
            - name: RABBITMQ_PORT
              value: "5672"
            - name: RABBITMQ_USERNAME
              valueFrom:
                secretKeyRef:
                  name: andikisha-secrets
                  key: rabbitmq-username
            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: andikisha-secrets
                  key: rabbitmq-password
            - name: ZIPKIN_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: andikisha-config
                  key: zipkin-endpoint
            - name: PAYROLL_SERVICE_HOST
              value: "payroll-service"
            - name: PAYROLL_SERVICE_GRPC_PORT
              value: "9084"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 45
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 5"]
