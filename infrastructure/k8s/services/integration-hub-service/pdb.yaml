apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: integration-hub-service
  namespace: andikisha
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: integration-hub-service
