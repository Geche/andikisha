apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: andikisha
  labels:
    app: audit-service
    app.kubernetes.io/name: audit-service
    app.kubernetes.io/part-of: andikisha-hr
spec:
  type: ClusterIP
  selector:
    app: audit-service
  ports:
    - name: http
      port: 8092
      targetPort: 8092
      protocol: TCP
