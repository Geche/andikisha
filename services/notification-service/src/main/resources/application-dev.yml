spring:
  datasource:
    url: jdbc:postgresql://localhost:5441/andikisha_notify
    username: andikisha
    password: changeme
  rabbitmq:
    host: localhost
    port: 5672
    username: andikisha
    password: changeme
  # MailHog for local development — catches all outbound email without sending
  mail:
    host: localhost
    port: 1025
    username: ""
    password: ""
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# SMS disabled in dev; api-key provided as empty string to satisfy Spring binding
app:
  notifications:
    sms:
      api-key: ""

logging:
  level:
    com.andikisha: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
