spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  rabbitmq:
    listener:
      simple:
        auto-startup: false
  data:
    redis:
      host: localhost
      port: 6379

grpc:
  server:
    port: -1