spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/andikisha_document
    username: andikisha
    password: changeme
  rabbitmq:
    host: localhost
    port: 5672
    username: andikisha
    password: changeme
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

grpc:
  client:
    payroll-service:
      address: static://localhost:9084

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

logging:
  level:
    com.andikisha: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
