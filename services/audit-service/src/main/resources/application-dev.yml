spring:
  datasource:
    url: jdbc:postgresql://localhost:5444/andikisha_audit
    username: andikisha
    password: changeme
  rabbitmq:
    host: localhost
    port: 5672
    username: andikisha
    password: changeme
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

logging:
  level:
    com.andikisha.audit: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
